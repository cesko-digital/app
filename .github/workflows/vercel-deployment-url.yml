name: Deployment URL
on:
  pull_request:
    branches: [master]
    types: [opened]
jobs:
  example_comment_pr:
    runs-on: ubuntu-latest
    name: Add deployment URL
    steps:
      - name: 'Comment PR'
        uses: actions/github-script@0.3.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { issue: { number: issue_number }, repo: { owner, repo }  } = context;
            const { GITHUB_HEAD_REF } = process.env;
            const escapedBranchName = GITHUB_HEAD_REF.replaceAll("/", "-");
            const deploymentUrl = `https://web-git-${escapedBranchName}-ceskodigital.vercel.app`;
            github.issues.createComment({ issue_number, owner, repo, body: `📌 PR se bude nasazovat sem: ${deploymentUrl}.` });
