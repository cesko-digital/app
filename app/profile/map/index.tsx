export const VolunteerMapPrefs = () => {
  return <Section>Here be dragons!</Section>;
};

const Section = ({ children }: { children: React.ReactNode }) => (
  <section className="text-lg max-w-prose">{children}</section>
);
