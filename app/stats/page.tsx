import { type Metadata } from "next";

export const revalidate = 300;

export const metadata: Metadata = {
  title: "Statistiky | Česko.Digital",
};

export default function Page() {
  // Page content is handled by @tabs route
  return null;
}
