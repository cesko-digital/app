"use client";

// This is just re-exporting the component with the client
// marker above. Once `react-lite-youtube-embed` adds `"use client"`
// to their codebase, we may drop this.
export { default } from "react-lite-youtube-embed";
